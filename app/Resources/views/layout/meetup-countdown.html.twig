<div class="meetup-countdown">
    <div class="row">
        <div class="col-lg-12 center">
            <h1>{{ meetups.data[0].name }}</h1>
            <hr>
            <h2>Starts in 05 days, 07 hours, 42 minutes, 36 seconds</h2>

            <p>{{ meetups.data[0].yes_rsvp_count }} coders already signed up, join them <a
                        href="{{ meetups.data[0].event_url }}">here</a>!</p>
        </div>
    </div>
</div>